package apitypes

import "github.com/puppetlabs/wash/plugin"

// EntrySchema describes an entry's schema, which
// is what's returned by the /fs/schema endpoint.
type EntrySchema = plugin.EntrySchema
